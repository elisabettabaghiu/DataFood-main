export interface CartItem {
  // Id del prodotto nel catalogo.
  productId: number;
  // Nome del prodotto.
  nome: string;
  // Prezzo unitario.
  prezzo: number;
  // Quantità presente nel carrello.
  quantita: number;
}
